const header=document.querySelector('.site-header');const progress=document.querySelector('.scroll-progress span');const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');const reveals=document.querySelectorAll('.reveal');
function onScroll(){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${max>0?scrollY/max*100:0}%`;header.classList.toggle('scrolled',scrollY>20)}
addEventListener('scroll',onScroll,{passive:true});onScroll();
toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -6%'});reveals.forEach(el=>io.observe(el));
document.getElementById('year').textContent=new Date().getFullYear();
document.getElementById('leadForm')?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget);const text=`Olá, AR Digital! Gostaria de solicitar uma análise inicial.%0A%0A*Nome:* ${encodeURIComponent(d.get('nome')||'')}%0A*Empresa/atividade:* ${encodeURIComponent(d.get('empresa')||'')}%0A*Segmento:* ${encodeURIComponent(d.get('segmento')||'')}%0A*Cidade:* ${encodeURIComponent(d.get('cidade')||'')}%0A*WhatsApp:* ${encodeURIComponent(d.get('whatsapp')||'')}%0A*Site/Instagram:* ${encodeURIComponent(d.get('site')||'')}%0A*Necessidade:* ${encodeURIComponent(d.get('mensagem')||'')}`;const numero='INSERIR_NUMERO_AR_DIGITAL';if(numero.startsWith('INSERIR_')){alert('Configure o número do WhatsApp no arquivo js/script.js antes da publicação.');return}window.open(`https://wa.me/${numero}?text=${text}`,'_blank','noopener')});
